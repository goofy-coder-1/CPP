#include <iostream>

int main() {
    
    char elements[] = {'a', 'b', 'c'}; 
    
    
    for (int i = 0; i < 3; i++)
    {
        
        std::cout << elements[i] << " ";
    }
    std::cout << '\n';
    return 0;
}